# Screenshots

UI captures referenced from the main [README](../README.md).

| File | Shows |
|------|-------|
| `scan_results.png` | Scanning a branch: declared indexes plus suggested indexes with priority levels |
| `compare_live_database.png` | Diffing suggestions against a live database: missing vs already covered |
| `export_suggestions_formatbased.png` | Generated index-creation script in the selected format (mongo shell / pymongo / PostgreSQL SQL) |

Reference them as: `![Description](screenshots/filename.png)`

When adding new captures, use a real repository rather than a synthetic one, and
crop out any connection strings, hostnames, or tenant database names before
committing.
